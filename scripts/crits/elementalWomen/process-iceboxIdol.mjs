import { processCritIcon } from "../../lib/process-crit-icon.mjs";

await processCritIcon("iceboxIdol", {
  backgroundColor: [146, 197, 250],
  dropWhiteHalo: true,
  copyToAssetDirectories: true,
});
