import { processCritIcon } from "../../lib/process-crit-icon.mjs";

// the seed clears the cream pocket enclosed between the arm and the hair
await processCritIcon("hoodedHush", {
  backgroundSeeds: [[587, 365]],
  dropWhiteHalo: true,
  copyToAssetDirectories: true,
});
