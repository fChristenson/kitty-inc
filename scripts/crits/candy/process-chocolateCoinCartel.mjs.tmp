import { processCritIcon } from "../../lib/process-crit-icon.mjs";

// the white showing through the net's mesh is the bag's own inside, not background
await processCritIcon("chocolateCoinCartel", { copyToAssetDirectories: true });
