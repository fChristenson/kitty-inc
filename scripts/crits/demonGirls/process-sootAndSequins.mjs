import { processCritIcon } from "../../lib/process-crit-icon.mjs";

// the gap between her arm and waist is walled in
await processCritIcon("sootAndSequins", {
  backgroundSeeds: [[714, 393]],
  copyToAssetDirectories: true,
});
